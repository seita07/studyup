<% if user_signed_in? %>
<%= form_with(model:[timepost, Comment.new]) do |form| %>
    <%= form.text_area :text ,:class => 'comment_area'%>
    <%= form.submit "コメントする" ,:class => 'comment_submit'%>
<% end %>
<% end %>